Several bugs, all rooted in the same underlying issue: draft/live status and video progress are not properly scoped per-user or per-role. Please fix all of the following:

1. DRAFT LESSONS ARE VISIBLE TO STUDENTS — when I toggle a lesson to "Draft" in the admin panel, it should immediately become invisible/inaccessible to any student account, appearing only to admins (in the admin panel, clearly marked as draft). Check the actual API/query that fetches lessons for the student-facing pages — it must filter out non-published lessons at the database query level, not just hide them visually on the frontend. A student should get a 404 or "not available" if they somehow try to access a draft lesson's direct URL.

2. VIDEO PROGRESS IS SHARED ACROSS ACCOUNTS INSTEAD OF PER-USER — when I paused a video on one account and logged in with a different email, it resumed from the same paused position. This means video watch-progress is being stored globally (maybe by lesson ID only) instead of being tied to the specific logged-in user's ID. Fix the progress-tracking table/logic so playback position is stored per (user_id, lesson_id) pair, never shared between different accounts.

3. ADMIN ACCOUNTS SHOULD NEVER SEE THE STORE/CART EXPERIENCE BY DEFAULT — currently everything is unlocked for admins, which is fine, but I need the Student/Admin toggle to actually mean something: when an admin switches the toggle to "Student" view, the system should show them EXACTLY what a real, non-owning student account would see — including needing to Add to Cart / Checkout to unlock modules, seeing "Draft" lessons hidden, etc. Right now toggling to Student still shows everything unlocked because the account is an admin underneath. Please make the toggle a genuine "preview as student" mode — when in that mode, the interface should behave and query data as if the admin owns nothing and has no special access, purely for testing purposes. Toggling back to Admin restores full access immediately.

Test each fix explicitly: (a) set a lesson to draft, confirm a student account can't reach it even via direct URL, (b) pause a video on one account, log in as a different account, confirm it starts from zero, (c) toggle an admin account to "Student" view and confirm the cart/checkout flow behaves exactly like a real non-owning student would see it.